services:
  vision_pipeline:
    build: .
    container_name: vision_pipeline

    stdin_open: true
    tty: true

    volumes:
      - ./outputs:/workspace/outputs
    
    ports:
      - "8000:8000"